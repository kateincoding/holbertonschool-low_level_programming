#include <stdio.h>

/**
 * main - Program that prints argv
 * @argc: number of argv
 * @argv: commands of program
 * Return: Always 0
 **/
int main(int argc, char **argv)
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
