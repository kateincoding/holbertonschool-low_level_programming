#include "holberton.h"

/**
 * mul - product of two inputs
 * @a: first number in ascii to evaluate
 * @b: second number in ascii to evaluate
 * Return: (1) if c iS uppercase (0) otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
